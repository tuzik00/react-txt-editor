import {
    Meta,
    ArgsTable,
} from '@storybook/addon-docs';

import ContextMenu from '../ContextMenu';

<Meta
    id="ContextMenu"
    title="Components/ContextMenu/Api"
/>

# ContextMenu

Документация по API для компонента ContextMenu

## Import

```typescript
import { ContextMenu } from '@citilink/content-editor';
```

## Props

<ArgsTable of={ContextMenu}/>

## Example

**itemsList** - массив кнопок

```typescript jsx
import React from 'react';
import { ContextMenu } from '@citilink/content-editor';
import { IconReplace } from '@/components/Icons';

const Component = () => (
    <ContextMenu
        itemsList={[
             <MenuButton
                label={'Заменить'}
                iconNode={<IconReplace />}
                onClick={() => {}}
            />
        ]}
    />
);

export default Component;
```
